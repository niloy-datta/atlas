# GitHub Actions

CI workflows are introduced in Phase 25 after the product integration-test topology and image strategy are defined. Local verification is currently authoritative through `scripts/verify.ps1` and `scripts/verify.sh`.

